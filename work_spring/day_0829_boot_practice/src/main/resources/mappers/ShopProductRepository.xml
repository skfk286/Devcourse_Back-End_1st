<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ycjung.shopping.model.repository.ShopProductRepository">

    <!-- shop_products 테이블의 모든 데이터를 조회하는 쿼리 -->
    <select id="selectAllProducts" resultMap="shopProductResultMap">
        SELECT
            product_id,
            product_name,
            description,
            price,
            stock_quantity,
            created_at,
            updated_at
        FROM shop_products
    </select>

    <!-- ShopProductDTO와 매핑될 resultMap 정의 -->
    <resultMap id="shopProductResultMap" type="com.ycjung.shopping.model.dto.ShopProductDTO">
        <result property="productId" column="product_id" />
        <result property="productName" column="product_name" />
        <result property="description" column="description" />
        <result property="price" column="price" />
        <result property="stockQuantity" column="stock_quantity" />
        <result property="createdAt" column="created_at" />
        <result property="updatedAt" column="updated_at" />
    </resultMap>
</mapper>